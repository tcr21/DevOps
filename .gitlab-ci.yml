stages:
  - build
  - test
  - deploy

build:
  stage: build
  image: python:3.8
  script: 
    - echo "Building..."
    - sudo apt install python3.8
    - python --version
    - pip install -r requirements.txt
  
test:
  stage: test
  script:
    - echo "Testing..."
    - python -m pytest

deploy:
  stage: deploy
  script:
    - echo "Deploying..."
