stages:
  - build
  - test
  - deploy

build:
  stage: build
  image: python:3.8
  script: 
    - echo "Building..."
    - pip --version
    - pip install -r requirements.txt
  
test:
  stage: test
  script:
    - echo "Testing..."
    - pip install pytest
    - pytest

deploy:
  stage: deploy
  script:
    - echo "Deploying..."
