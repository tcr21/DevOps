stages:
  - build
  - test
  - deploy


build:
  stage: build
  script:
    - echo "Building...."
    - ls -a
    - python --version
    - pip3 list

test:
  stage: test
  script:
    - echo "Testing..."
    - pip3 list
    - python3 -m pytest

deploy:
  stage: deploy
  script:
    - echo "Deploying..."
    - pip3 list
    - flask run
